(() => {
  let productTitle = 'My amazing product';
  const productDescription = "Product's Description";
  let summary = `
    title: ${productTitle}
    description: ${productDescription}
  `;

  console.log('productTitle', productTitle);
  console.log('summary', summary);
})();
